---

 - name: download binary
   get_url:
     url: "{{ elm_download_url }}"
     dest: "{{ elm_download_tmp }}"
   changed_when: false

 - name: extract from archive
   become: yes
   unarchive:
     src: "{{ elm_download_tmp }}"
     dest: /usr/local/bin
     remote_src: yes
     owner: "{{ ansible_user }}"
     group: "{{ ansible_user }}"
     mode: 0744

