---

 - name: add ppa repository for swi-prolog
   become: yes
   apt_repository:
     repo: 'ppa:swi-prolog/stable'
     codename: "{{ ubuntu_codename }}"

 - name: install encryption packages
   become: yes
   apt:
     name: "{{ packages }}"
   vars:
     packages:
     - swi-prolog
