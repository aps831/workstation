---

 - name: install packages
   become: yes
   apt:
     name: "{{ packages }}"
     state: present
   vars:
     packages:
     - haskell-stack

 - name: upgrade stack
   become: yes
   shell: stack upgrade --binary-only --binary-version {{ stack_version }}

