---
name: Trunk Check
on:
  pull_request: {}
permissions:
  contents: read
  issues: read
  pull-requests: read
jobs:
  check:
    name: trunk check
    uses: aps831/workflows/.github/workflows/trunk.yaml@master
