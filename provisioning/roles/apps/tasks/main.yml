---

 - name: install application packages
   become: yes
   apt:
     name: "{{ packages }}"
   vars:
     packages:     
     - libreoffice
     - imagemagick
     - sound-juicer
     - inkscape
     - easytag
