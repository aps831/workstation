---

 - name: install application packages
   become: yes
   apt:
     name: "{{ packages }}"
   vars:
     packages:
     - shotwell
     - libreoffice
     - imagemagick
     - sound-juicer
     - inkscape
