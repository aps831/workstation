---

 - name: add repository
   become: yes
   apt_repository:
     repo: ppa:jlbarriere68/noson-app
     codename: "{{ ubuntu_codename }}"
     state: present
     update_cache: yes

 - name: install noson
   become: yes
   apt:
     name: noson-app
     state: present
