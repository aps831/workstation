---

 - name: install packages
   become: yes
   apt:
     name: "{{ packages }}"
     state: present
   vars:
     packages:
     - python3-gpg
     - dropbox
     # - nemo-dropbox this is causing problems with a prompt that debconf can't answer
