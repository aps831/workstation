---

 - name: install packages
   become: yes
   apt:
     name: "{{ packages }}"
     state: present
   vars:
     packages:
     - python-gpg
     - python3-gpg     
     - dropbox
     - nemo-dropbox
