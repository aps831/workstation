---

 - name: run playbook tasks
   hosts: local
   vars:
     ansible_user: ansible
   roles:
     - role: gogs

   pre_tasks:
     - name: update repository caches
       become: yes
       apt: update_cache=yes
       changed_when: False
